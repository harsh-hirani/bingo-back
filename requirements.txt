# Django & Core
django>=4.2
django-environ
dj-database-url
djangorestframework

# ASGI + websocket stack
channels
daphne

# Redis channel layer
channels-redis
# Database (Postgres)
djangorestframework-simplejwt

psycopg2-binary

# CORS (optional but recommended)
django-cors-headers

# Deployment helpers
gunicorn

# Optional utilities
whitenoise
